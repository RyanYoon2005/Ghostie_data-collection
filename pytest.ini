[pytest]
testpaths = tests
addopts = --self-contained-html -v
markers =
    unit: unit tests (isolated, all external calls mocked)
    integration: integration tests (full request/response cycle, no real network)
